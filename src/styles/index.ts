import { createStitches } from '@stitches/react';

export const {
  styled,
  config,
  theme,
  createTheme,
  css,
  globalCss,
  getCssText,
  keyframes,
  prefix,
  reset,
} = createStitches({
  theme: {
    colors: {
      rocketseat: '#8257e6',
    },
  },
});
